<div class="container mt-3">
  <h1>Welcome to Mordor!!! 😈</h1>

  <% @characters.each do |character| %>
    <!-- Sigular paths always leave to show page -->
    <div class="card p-4 m-5 text-center"><%= link_to character.name.capitalize, character_path(character.id), class: 'text-decoration-none' %></div>
  <% end %>
</div>
